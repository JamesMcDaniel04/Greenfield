# Public — exposed to browser
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...

# Edge-function secrets (ANTHROPIC_API_KEY, STRIPE_*, etc.) are set via
# `supabase secrets set`, not here. See greenfield/README.md.
